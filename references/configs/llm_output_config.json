{
    "type": "object",
    "properties": {
        "answer": {
            "type": "array",
            "items": {"type": "string", "pattern": "^[A-Z]$"},
            "minItems": 1,
            "maxItems": 1
        }
    },
    "required": ["answer"]
}